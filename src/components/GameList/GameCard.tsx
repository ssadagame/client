import styled from 'styled-components';

const Back = styled.div`
  border: 1px solid;
  width: 100px;
  height: 100px;
`;
const GameCard = () => {
  return <Back>안녕</Back>;
};

export default GameCard;
